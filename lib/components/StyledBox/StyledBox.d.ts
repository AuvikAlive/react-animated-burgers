import { ButtonWidth } from '../Button';
declare type props = {
    buttonWidth: ButtonWidth;
};
export declare const StyledBox: import("styled-components").StyledComponent<"div", any, props, never>;
export {};
