import { BarColor, ButtonWidth, IsActive } from '../Button';
export declare type LineProps = {
    barColor: BarColor;
    buttonWidth: ButtonWidth;
    isActive: IsActive;
};
export declare const StyledLines: import("styled-components").StyledComponent<"span", any, LineProps, never>;
