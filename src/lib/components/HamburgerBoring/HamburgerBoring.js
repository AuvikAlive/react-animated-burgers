import React from 'react'
import { Button } from '../button/Button'
import { StyledLinesBoring } from './StyledLinesBoring'

const HamburgerBoring = props => <Button {...props} Lines={StyledLinesBoring} />

export default HamburgerBoring
