import styled from 'styled-components'

export const StyledBox = styled.div`
  width: ${({ buttonWidth }) => buttonWidth};
  height: ${({ buttonWidth }) => `calc(${buttonWidth} * 0.6)`};
  display: inline-block;
  position: relative;
`

StyledBox.displayName = 'StyledBox'
