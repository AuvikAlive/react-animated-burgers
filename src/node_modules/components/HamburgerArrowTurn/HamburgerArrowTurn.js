import React from 'react'
import { Button } from '../button/Button'
import { StyledBox } from '../styledBox/StyledBox'
import { StyledLinesArrowTurn } from './StyledLinesArrowTurn'

const HamburgerArrowTurn = props => {
  const { isActive, barColor } = props

  return (
    <Button {...props}>
      <StyledBox>
        <StyledLinesArrowTurn {...{ isActive, barColor }} />
      </StyledBox>
    </Button>
  )
}

export default HamburgerArrowTurn
