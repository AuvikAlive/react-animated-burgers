import React from 'react'

import Button from '../Button/Button'
import Box from '../Box/Box'
import LinesArrowTurn from './LinesArrowTurn'

const HamburgerArrowTurn = props => (
  <Button {...props}>
    <Box>
      <LinesArrowTurn isActive={props.isActive} barColor={props.barColor} />
    </Box>
  </Button>
)

export default HamburgerArrowTurn
