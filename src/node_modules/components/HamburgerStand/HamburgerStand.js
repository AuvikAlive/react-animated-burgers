import React from 'react'
import { Button } from '../button/Button'
import { StyledLinesStand } from './StyledLinesStand'

const HamburgerStand = props => {
  const { isActive, barColor } = props

  return (
    <Button {...props}>
      <StyledLinesStand {...{ isActive, barColor }} />
    </Button>
  )
}

export default HamburgerStand
